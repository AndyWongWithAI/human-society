name: 部署知识图谱

on:
  push:
    branches: [main]
    paths:
      - 'L0-physical/**'
      - 'L1-definitions/**'
      - 'L2-bridging/**'
      - 'L3-deductions/**'
      - 'L4-composites/**'
      - 'scripts/export_graph.py'
      - 'visualization/index.html'
      - 'api.py'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安装依赖
        run: pip install pyyaml

      - name: 导出图谱数据
        run: python scripts/export_graph.py

      - name: 部署可视化文件
        uses: easingthemes/ssh-deploy@v6
        with:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_KEY }}
          ARGS: "-avz --delete"
          SOURCE: "visualization/"
          REMOTE_HOST: "124.71.219.208"
          REMOTE_USER: ${{ secrets.SSH_USER }}
          TARGET: "/var/www/human-society.intelab.cn/"

      - name: 部署 API 代码
        uses: easingthemes/ssh-deploy@v6
        with:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_KEY }}
          ARGS: "-avz"
          SOURCE: "api.py"
          REMOTE_HOST: "124.71.219.208"
          REMOTE_USER: ${{ secrets.SSH_USER }}
          TARGET: "/opt/services/human-society-api/"

      - name: 重启 API 服务
        uses: appleboy/ssh-action@v1
        with:
          host: "124.71.219.208"
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: systemctl restart human-society-api
